"use strict";

const assert = require("assert");

module.exports = function () {
    
    return {
        save: async (entry) => {
                //AWS implementation here
        },
        find: async (frameNumber) => {
            return null;
        }
    }
}